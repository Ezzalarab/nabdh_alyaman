# Backlog — باكإند

- إشعار موضوع (topic) جماعي عبر قرار منتج بديلاً عن العميل القديم.  
- `GET /app-content/home` موحّد بدلاً من JSON في `app-config` فقط (إن رُغب).  
- مزيد من التحليلات (تفريق بحث يدوي/جغرافي في لوحة مشرف متقدمة).

أضف عناصر جديدة هنا مع ربط بتذكرة/مرحلة واضحة عند ظهورها.
